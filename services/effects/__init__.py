"""Effects resolution: behavioral-hash identity, selection/ordering, and the
fork/eth_call simulation harness (EFFECTS_RESOLUTION_SPEC.md). Feature-flagged
via PSAT_EFFECTS_STAGE. Phase 5 (§5.2) bridges *proven* verdicts into registry
claims (``claims_bridge``) so the frontend renders them as observable labels;
the score still does not consume verdicts (deferred to SCORING_INVARIANTS.md)."""
