# Proposal impact surface

Mode: Operate

## THESIS

One comparison should answer what a proposal changes, which pinned state it was
compared with, and why PSAT believes each consequence without allowing a
hypothesis to read as live state.

## OWN-WORLD

Inherit PSAT's restrained dark analytical interface, fine rules, compact status
language, Instrument Sans typography, teal observed-state semantics, and amber
scenario semantics. This is an extension, not a new visual identity.

## STORY

Lead with scenario deltas because that is the decision task. Follow with the
observed proposal/operation record that establishes reachability and timing.
End with coverage limitations so unsupported semantics cannot disappear into an
empty result. Proof, ordered actions, assumptions, and downstream claims expand
inline at the point where they matter.

## FIRST VIEWPORT

The page title and scope legend establish observed versus hypothetical before a
single value appears. The first table compares pinned baseline and proposed
values with scenario step and proof in the same scan line. On mobile, each row
becomes a labeled two-column record without horizontal clipping.

## FORM

Seed key: `33275d1f`; recorded choice: `pick`. The user's prior decision to
prefer tabular views over mappings pins the grounded investigation-ledger form
over the dealt ornamental structures. Quality bar: the incumbent
`site/e2e/visual-baseline.spec.js-snapshots/company-surface-linux.png` for density,
hierarchy, and visual-system fidelity. Code-led narrow extension; no comp.
